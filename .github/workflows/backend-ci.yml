name: Backend CI

on:
  pull_request:
    paths:
      - "backend/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # TODO: Node.js/Python 등 런타임 확정 후 dependency 설치 스텝 작성
      - name: Install dependencies (placeholder)
        run: echo "TODO: install backend dependencies"

      - name: Run tests (placeholder)
        run: echo "placeholder test for backend"
